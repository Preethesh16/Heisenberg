{
  "text": "Friction doesn't care which way the block moves. The belt slips forward relative to the block, so friction on the block acts forward — it opposes the relative slipping between the two surfaces, not the block's velocity.",
  "lang": "en"
}
